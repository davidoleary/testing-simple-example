{
  "name": "test-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./*.spec.js --recursive --reporter spec",
    "test-watch": "mocha --watch ./*.spec.js --recursive --reporter spec"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "supertest": "^3.0.0"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
